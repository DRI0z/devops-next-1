version: '3'
services:
  devops-next-1:
    image: ghcr.io/dri0z/devops-next-1/devops-next-1:test
    container_name: devops-next-1-test
    restart: always
    ports:
      - "8080:3000"
    environment:
      - NODE_ENV=production